<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Two permissions, both earned.

        CAMERA is the scanner. INTERNET is optional sync — the app is fully usable with it
        denied at the network level, because the device's own database is the source of truth.

        The legacy app asked for WRITE_SETTINGS, WRITE_EXTERNAL_STORAGE, SET_ALARM, and
        RECEIVE_BOOT_COMPLETED. None of that is needed: exports go through the system share
        sheet, and the attendance windows are computed from the clock rather than scheduled as
        alarms, so there is nothing to re-arm after a reboot.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".AttendanceApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Attendance">

        <!--
            A single activity. There are no exported receivers at all, which closes the hole the
            legacy app left open: every one of its eleven receivers was exported without a
            permission, so any installed app could broadcast attendance for any student.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Attendance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
