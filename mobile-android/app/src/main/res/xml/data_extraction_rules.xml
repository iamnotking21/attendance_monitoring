<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!-- Attendance data is a roster of minors. It is not copied into cloud backup or handed to
         a new device by transfer; the app has its own explicit backup and sync instead. -->
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
